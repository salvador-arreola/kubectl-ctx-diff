# Tasks

## Phase 1 - MVP ✓
- [x] cobra root command + --context-1 / --context-2 flags
- [x] fetch and diff ConfigMaps between two namespaces
- [x] fetch and diff Secrets (show keys only, never values)
- [x] fetch and diff resource requests/limits per Deployment
- [x] fetch and diff env vars per Deployment
- [x] colored table output (added/removed/changed)
- [x] --output json flag

## Phase 2
- [ ] --filter flag (only show specific resource types)
- [ ] krew manifest for distribution
- [ ] GitHub Actions release pipeline
